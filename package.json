{
  "name": "simple-api",
  "version": "0.1.0",
  "scripts": {
    "start": "node src/index.js",
    "lint": "eslint .",
    "test": "jest --runInBand",
    "ci": "npm run lint && npm test"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
